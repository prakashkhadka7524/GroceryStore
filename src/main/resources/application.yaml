spring:
  datasource:
    username: grocery_store
    password: grocery_store
    url: jdbc:mysql://localhost:3306/grocery_store

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always